# 教学 5 · 插件（自己加首页小卡片，跟你的 AI 商量着做）

> 插件 = **一张你自己加的首页小卡片**，比如股票、服务器状态、粉丝数、任何接口的数字。
> 它不是代码，是**一段 JSON 描述**（App 只按描述去请求你指定的 https 地址、取一个值显示出来）。
> 所以：**你可以让自己的 AI（豆包 / 腾讯马维斯 / ChatGPT）帮你写这段 JSON**，写好了粘回 App 就行。

---

## 最快用法：加一个股票卡（只填代码）

设置 → **插件** → 「② 加一个股票卡」→ 填 `600519`（或 `sh000001` 看上证指数）→ 加。
首页就会出现一张卡片：大字是现价，下面一行是涨跌，**红涨绿跌**，每 60 秒自动刷新，点一下也刷。

---

## 通用用法：跟 AI 商量一个插件

1. **设置 → 插件 → 「① 复制插件规格（给 AI 看）」**
2. 把复制到的规格**整段粘给你的 AI**，末尾写清你要什么，例如：
   > 我要看：上证指数今天的点位 + 涨跌幅
3. AI 会给你**一段 JSON**（只输出 JSON）
4. 回到 **设置 → 插件 → 「③ 粘贴 AI 给的插件 JSON」** → 装上
5. 列表里点「试」先看拉到的值对不对 → 点「上首页」加到首页

---

## 插件 JSON 格式（AI 写的就是这个）

```json
{
  "id": "sh000001",
  "name": "上证指数",
  "icon": "📈",
  "url": "https://qt.gtimg.cn/q=sh000001",
  "enc": "gbk",
  "pick": "split:~:3",
  "templ": "{v}",
  "sub": "{chg}",
  "chg_pick": "split:~:32",
  "refresh": 60
}
```

| 字段 | 说明 |
|---|---|
| `url` | 要请求的地址（**必须 https**，公开、无需登录的接口）|
| `enc` | `utf-8` 或 `gbk`（腾讯行情是 gbk）|
| `pick` | 取值方式，三选一：<br>· `split:分隔符:第几个`（例 `split:~:3` = 按 `~` 切开取第 4 段）<br>· `json:路径`（例 `json:data.price`、`json:list.0.name`）<br>· `regex:正则:第几组`（例 `regex:现价[:：]\s*([\d.]+):1`）|
| `templ` | 大字模板，`{v}` 会被替换成取到的值 |
| `sub` | 小字模板，可用 `{v}` 和 `{chg}` |
| `chg_pick` | 可选：哪个字段是涨跌幅（数字）→ 用来红涨绿跌 |
| `refresh` | 刷新间隔（秒，15~3600）|

---

## 常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| 卡片显示「取数失败」 | url 不通 / 需要登录 / 被墙 | 换成公开接口；确认手机能打开这个地址 |
| 值取出来是空的 | `pick` 写错了 | 用「试」按钮看拉到什么，再让 AI 按真实返回改 `pick` |
| 显示乱码 | `enc` 不对 | 中文接口多半是 `gbk`，JSON 接口是 `utf-8` |
| 数值太长显示不全 | 模板太长 | 让 AI 只取关键数字 |
| 想删掉 | — | 插件页点「删」；首页那张卡会显示「插件已删除」，长按卡片可删 |

---

## 边界（诚实说）

- 插件**只能显示数据**，不能执行代码、不能下单、不能读你手机里的东西（这是刻意的安全设计）
- 只支持 **公开、免登录** 的 https 接口；需要密钥的接口请自己权衡（密钥会存在 App 内）
- 想加"真正能点进去操作"的复杂功能 → 那是 App 本身的功能，不是插件
