# 放在站点 server 块里（或单独的 sites-available 文件）—— 用域名访问
location = /linji/bridge.php {
    proxy_pass http://127.0.0.1:16632;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    client_max_body_size 512k;
}
location = /linji/bridge {
    proxy_pass http://127.0.0.1:16632;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    client_max_body_size 512k;
}

# ——— 只用 PHP 的话，不需要上面两行反代，静态 + php-fpm 就够 ———
# ——— 想「直接填 IP 就用」：把下面这块加进 80 端口的默认站点 ———
#
# server {
#     listen 80 default_server;
#     server_name _;
#     location = /linji/bridge.php { proxy_pass http://127.0.0.1:16632; proxy_set_header Host $host; client_max_body_size 512k; }
#     location ^~ /linji/ { root /www/wwwroot/你的站; }
#     location / { return 404; }
# }
