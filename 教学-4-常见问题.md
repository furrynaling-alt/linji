# 教学 4 · 常见问题（照着查）

## 一、配对类

| 现象 | 原因 | 解决 |
|---|---|---|
| App 桥显示「同步失败」 | 服务器地址/令牌不对 | ① 桥地址要 `https://域名/linji/bridge.php`；② 令牌与服务器 `token.txt`/`$TOKEN` **逐字一致** |
| 服务器日志出现 `bad-token` | 同上（口令不一致） | 重填令牌，注意别带空格 |
| 服务器收到请求但 **404** | nginx 没有 `/linji/` 规则 | 站点配置加 `location ^~ /linji/ { root /www/wwwroot/你的站; }` → `nginx -t && systemctl reload nginx` |
| SSH 报 `Auth fail`/`Permission denied` | 公钥没装或权限错 | 重跑「一键安装命令」；`~/.ssh`=700、`authorized_keys`=600 |
| SSH 报 `EPERM` | App **缺 INTERNET 权限**（v2.34 起已修）| 升级 APK |
| SSH 卡"配对中…"或 `Read timed out` | 裸 IP 高端口被丢包 | 让 SSH 走 **443**（见教学 2）|
| 手机数据一直不上来 | 桥**开关没开** 或 App 被系统冻结 | 装 **v2.42+**（桥是常驻前台服务，通知栏会有「棂记 · 自动同步中」，别清掉）；确认卡片显示 ✅ |

## 二、检查更新类

| 现象 | 原因 | 解决 |
|---|---|---|
| 点「检查更新」说失败 | 服务器地址空/填错；或服务器上没放 `version.json` | 填自己的域名或 IP；把 `version.json` + APK 放到 `你的站/linji/` |
| 填了 IP 还是失败 | ① 明文 http 被网络拦 ② 老版本缺 INTERNET 权限 | 用域名 + https；或升级 APK |
| 更新清单要写什么 | 见本仓 `version.json.示例` | `versionCode` 必须**大于**当前 build 才会提示更新 |

## 三、数据与隐私

| 问题 | 说明 |
|---|---|
| 我的数据发到哪了？ | **只发到你 App 里填的那台服务器**。App 里「服务器」留空时用的是作者的默认地址 —— 想自己掌控就填自己的 |
| 服务器能看到什么？ | 你允许它看的：电量、屏幕状态、纪念日卡片、待办、睡眠起止与时长。**有没有开桥由你控制**，关掉就完全离线 |
| 私钥会不会上传？ | **不会**。私钥只存在 App 私有目录；上传的只有公钥（就是给服务器认的那一半） |
| 别人能用我的服务器吗？ | 能，但要么给他令牌、要么给他单独一份 `bridge.php`。**别把你的令牌公开发出去** |

## 四、后台保活（国产系统特别爱杀后台）

- **v2.42 起**：桥跑在**前台服务**里，通知栏有一条低优先级通知「棂记 · 自动同步中」
- 想让同步最稳，再做两件事：
  1. 系统设置 → 电池 → 棂记 → 允许**后台高耗电 / 自启动 / 关联启动**（vivo 在「i管家 → 自启动」）
  2. 最近任务里**别上滑清掉棂记**（清掉后通知消失，同步会停，下次打开 App 自动恢复）
- 打开 App / 回到前台会**立刻同步一次**，所以哪怕被杀了，你一看手机就会补上

## 五、还是一头雾水？

把这段话整段复制给你的 AI（豆包 / 腾讯马维斯 / ChatGPT 都行），它会一步步带你做：

> 你是我的服务器运维助手。我要在自己的服务器上部署「棂记」的手机↔服务器同步。
> 我的服务器 IP/域名：____，SSH 端口：____，用户：root。
> 请按 https://github.com/furrynaling-alt/linji 的 README 和教学 1~4，给我**逐条命令 + 预期输出**，
> 先做 App 桥（bridge.php + nginx location），再做 SSH 公钥（走 443 分流），出错按排错表处理。
