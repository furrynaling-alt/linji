# 教学 1 · App 桥（手机 ↔ 服务器 自动同步）—— 最稳的一条路

> 桥是什么：手机**主动**把状态（电量/睡眠/待办/纪念日）发给服务器，每 20 秒一次；
> 手机在移动网络后面，服务器连不进去，所以必须手机"往外发"。
> 这条走 **HTTPS 443**，跟你在浏览器下东西同一条路，比 SSH 直连稳得多。

---

## 你要准备（2 样）

1. **服务器上一份桥的文件**（二选一）
   - PHP 版：把 `bridge.php` 上传到网站目录的 `linji/` 里（宝塔里拖一下即可）
   - Node 版：见本仓 `server.js`（要跑 `pm2 start server.js --name linji-bridge`）
2. **一个口令**：自己定一串没人猜得到的（例：`my-linji-2026`）。PHP 版写在 `bridge.php` 顶部的 `$TOKEN`；Node 版写在 `token.txt`

---

## 配对就 3 步（App 里）

| 步 | 在哪 | 填什么 |
|---|---|---|
| ① | 设置 → **服务器** | 填你服务器的**域名**（推荐）或 **IP**。留空 = 用默认地址 |
| ② | 设置 → **App 桥** → 点「设置令牌」 | 桥地址留默认；**令牌**填你上面定的那串 |
| ③ | 回到 App 桥 → **打开开关** → 点「**立即同步一次**」 | 看到"同步完成"= 通了 |

**通了之后**：不用再管它。桥每 20 秒自动同步，**联网恢复、开机、打开 App、回到前台**都会立刻补一次。

---

## 怎么确认真的通了（两边都能看）

- **App 里**：App 桥卡片上会写 `状态：✅ 自动同步中（每 20 秒）` + `上次同步：刚刚`
- **服务器上**：
```bash
# Node 版
curl -s http://127.0.0.1:16632/sleep     # 人话：上次一觉多久、近7次平均
curl -s http://127.0.0.1:16632/status    # 原始 JSON
# PHP 版
curl -s -X POST https://你的域名/linji/bridge.php -H 'X-Linji-Token: 你的口令' -d '{}'
```
最后一个应当返回 `{"cmds":[]}` 之类的 JSON —— 有这个回包就说明桥活着。

---

## 从服务器往手机发东西（待办 / 通知）

手机每 20 秒来取一次指令，所以你在服务器上"塞一条"，手机最多 20 秒后弹出来：

```bash
# Node 版
curl -s "http://127.0.0.1:16632/cmd?cmd=notify&title=棂记&text=该睡了"
curl -s -X POST http://127.0.0.1:16632/cmd -H 'Content-Type: application/json' \
     -d '{"cmd":"todo","text":"买牛奶"}'
```
手机端会：弹通知 / 进「记录 → 待办」列表（点一下=完成，服务器能读到完成状态）。

---

## 排错

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 服务器日志里 `bad-token` | 两边口令不一致 | 服务器 `token.txt` / `$TOKEN` 与 App 里填的**逐字一致**（别带空格） |
| 「同步失败」 | 桥地址不对 | 地址必须是 `https://你的域名/linji/bridge.php`；用**IP** 时是 `http://IP/linji/bridge.php`（明文，有些网络会拦） |
| 服务器收到请求返回 **404** | nginx 没配 | 站点配置里要有一行 `location ^~ /linji/ { root /www/wwwroot/你的站; }`，改完 `nginx -t && systemctl reload nginx` |
| 一直"没同步"，要手动点才行 | 桥开关没开 / App 被系统冻结 | ① 确认开关是开的（卡片上应显示 ✅ 自动同步中）② 装 **v2.42 以上**：桥是常驻前台服务，通知栏会有一条「棂记 · 自动同步中」，**别清掉它** |
| 手机一直上传，但服务器看不到 | 你连的是**默认服务器**（作者的） | 设置 → 服务器 填**你自己的**域名/IP |
