# 教学 3 · 没有域名怎么办（只有 IP 也能用）

> 域名**不是必需品**。缺的从来不是域名，而是"一条从你所在网络到服务器的稳路"。

| 你的情况 | 怎么办 | 花钱 |
|---|---|---|
| 服务器在**国内**（阿里/腾讯云） | App 里**直接填 IP**：`你的IP:80`（或 `IP:443`）→ 桥/SSH 都通 | 0 |
| 服务器在**海外** + 人在国内 | 用**免费域名**（下面 3 步）绑 Cloudflare → 走 443，跟你下东西同一条路 | **0**（域名免费、CF 免费套餐够用）|
| 连域名都不想要、也不想开端口 | **ZeroTier 组网**：手机和服务器各装一个客户端，手机直接访问服务器内网 IP | 0 |

> 备案只管**国内服务器**。海外服务器用免费域名**不需要备案**。

---

## 免费域名 + Cloudflare（3 步，全免费）

1. 去免费域名服务商注册一个（例如 DigitalPlat FreeDomain 的 `*.dpdns.org`、DuckDNS 的 `*.duckdns.org`、或 eu.org）
2. 登录 Cloudflare → 添加站点 → 按提示把域名的 NS 指过去 → 建一条 **A 记录**指向你的服务器 IP（**橙色云** 开启 = 走 CF）
3. 服务器上按本仓 README 第 3 节配好 nginx + 证书（CF 可签发源站证书，或直接用 CF 的 SSL 灵活模式）

App 里就填你的免费域名（例如 `https://你的名字.dpdns.org`）。

---

## ZeroTier 组网（免端口、免域名、免证书）

1. 服务器和手机各装 ZeroTier 客户端，加入同一个网络（自己建的）
2. 之后手机能像局域网一样访问服务器的**内网 IP**（ZeroTier 给的 `10.x.x.x`）
3. App 里服务器地址填那个内网 IP 即可（SSH 直接连 `10.x.x.x:22`）

⚠️ 只用于**自己组网/自己设备互联**；不要拿它去给别人翻墙或共享跨境线路（违法且平台会封号）。

---

## 关于 tor 洋葱地址（.onion）—— 不建议、也不做

- 它确实能"无域名全球寻址"，但**在国内连 Tor 需要先过审查（网桥/obfs4）**，属于翻墙工具；
- 而且慢、丢包，不适合做日常同步。
- 所以本项目的定位是：**用 IP 或免费域名 + 443**，足够稳、也不碰红线。

---

## 一句话对照

- **只为"手机↔服务器同步/待办"** → 用 **App 桥**（走 443，最稳）
- **要"手机执行服务器命令"** → **SSH**（海外服务器记得走 443，见教学 2）
- **服务器在国内** → 直接填 IP，全都能用
